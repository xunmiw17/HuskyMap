// The ColoredEdge type represents a colored edge with two endpoints.
export interface ColoredEdge {
    x1: number;
    y1: number;
    x2: number;
    y2: number;
    color: string;
    key: number
}